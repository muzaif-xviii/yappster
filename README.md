# Chat App

A real-time chat application using Node.js, Express, Socket.IO, and MongoDB.

## Setup

1. Clone the repo
2. Install dependencies: `npm install`
3. Create a `.env` file with your MongoDB URI
4. Run the server: `npm start`
